# Abandoned — Supabase setup

The Supabase production path is **stopped**.

Use [NODE-PRISMA-BACKEND.md](./NODE-PRISMA-BACKEND.md) and `server/` instead.

Do not run `supabase link`, Edge deploys, or set `VITE_SUPABASE_*`.
